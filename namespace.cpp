#include<iostream>
//std is predefined namespace which contains class istream with object cin and class ostream with object cout
//so we have done this program without using whole namespace, only used std for cout
int main()
{
    std::cout<<"Without namespace"<<"\n";
    return 0;
}